import torch
import torch.optim as optim

# optim.Adam

# class FAdam(optim.Optimizer):
#     def __init__(self, params: Iterable[Tensor] | Iterable[Dict[str, Any]], defaults: torch.Dict[str, torch.Any]) -> None:
#         super().__init__(params, defaults)

optim.SGD